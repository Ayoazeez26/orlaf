export const JWT_MODULE_OPTIONS = "JWT_MODULE_OPTIONS"

/**
 * Injection token for the validated JWT config object.
 * Use @Inject(JWT_CONFIG) in services that need raw config values.
 */
export const JWT_CONFIG = "JWT_CONFIG"
