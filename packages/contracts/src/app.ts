/**
 * @sable/contracts — app metadata (public)
 */

export interface AppInfoResponse {
  name: string;
  version: string;
  support_email: string;
  terms_url: string;
  privacy_url: string;
  licenses_url: string;
  acknowledgements_url: string;
}
